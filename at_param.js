/**
 * Função que cumprimenta uma pessoa
 * 
 * @param {object} person Objeto representando uma pessoa
 * @param {string} person.name Nome da pessoa
 */
function greet(person) {
  console.log(`Hello ${person}`);
}
